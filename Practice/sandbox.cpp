#include <vector>
#include <set>
#include <iostream>
using namespace std;

int main(){
    int number;

    do{
        cout << "Enter 1 or 2: ";
        cin >> number;
    }while (number != 1 && number != 2);
}